# Copie para .env e ajuste conforme o ambiente
# Em desenvolvimento local (fora do Docker), aponte para o Flask local
VITE_API_URL=http://localhost:5000

# Em produção dentro do Docker Compose, o Nginx proxy /api/ → backend:5000
# e você pode deixar VITE_API_URL vazio ou apontar para o domínio real
