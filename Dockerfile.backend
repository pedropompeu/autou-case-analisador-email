# Multi-stage build para otimização de imagem
# Stage 1: Build do Frontend React SPA
FROM node:20-alpine AS frontend-builder
WORKDIR /frontend
COPY frontend/package*.json ./
RUN npm ci --quiet
COPY frontend/ ./
RUN npm run build

# Stage 2: Compilação de dependências Python
FROM python:3.11-slim AS builder
WORKDIR /build
RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc \
    && rm -rf /var/lib/apt/lists/*
COPY requirements.txt .
RUN pip install --user --no-cache-dir -r requirements.txt

# Stage 3: Imagem final de execução
FROM python:3.11-slim
WORKDIR /app

# Criar usuário não-root para segurança
RUN useradd -m -u 1000 appuser

# Copiar dependências instaladas do builder
COPY --from=builder /root/.local /home/appuser/.local

# Garantir que scripts em .local estão no PATH
ENV PATH=/home/appuser/.local/bin:$PATH
ENV PYTHONPATH=/app:$PYTHONPATH

# Copiar código da aplicação backend e templates/static de fallback
COPY backend/ ./backend/
COPY templates/ ./templates/
COPY static/ ./static/
COPY wsgi.py ./wsgi.py
COPY app.py ./app.py
COPY celery_worker.py ./celery_worker.py

# Copiar o build do Frontend React SPA para o container do backend
COPY --from=frontend-builder /frontend/dist ./frontend/dist

# Ajustar permissões
RUN chown -R appuser:appuser /app /home/appuser/.local

USER appuser

# Expor porta
EXPOSE 5000

# Health check
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
    CMD python -c "import requests; requests.get('http://localhost:5000/health', timeout=2)"

# Comando para produção com Gunicorn
CMD ["gunicorn", "--bind", "0.0.0.0:5000", "--workers", "4", "--timeout", "120", "--access-logfile", "-", "--error-logfile", "-", "wsgi:app"]
